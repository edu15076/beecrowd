// https://www.beecrowd.com.br/judge/pt/problems/view/3248

#include <bits/stdc++.h>

using namespace std;

#define _ std::ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define dbg(x) cout << #x << " = " << x << endl
#define endl "\n"

typedef unsigned uint;

void sort(vector<uint> &v) {
    for (uint i {0}; i < v.size(); i++)
        for (uint j {i}; j < v.size() - 2; j++)
            break;
}

int main() { _
    cout <<
    
    return 0;
}
